# Core
httpx>=0.27          # ATS API calls (Greenhouse/Lever/Ashby/Workable)
requests>=2.31
pydantic>=2.7        # profile / job data models
python-dotenv>=1.0   # load .env

# Storage / export
openpyxl>=3.1        # Excel export
pandas>=2.2

# Resume parsing (M1)
pdfplumber>=0.11     # read resume PDFs
python-docx>=1.1     # read/write .docx

# Browser submit fallback (M5)
playwright>=1.44

# AI brain (used when running WITH an API key; CLI path needs no SDK)
anthropic>=0.40

# Web dashboard (M7, later)
fastapi>=0.111
uvicorn>=0.30
